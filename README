Current Awesome Stats:
Google Closure Compiler (simple optimizations): 7.68KB (3KB gzipped)

Awesome is awesome because it loads almost instantly, allowing you to perform common JavaScript tasks without annoying load-time lag. My goal is to perform as many common jQuery and plugin methods as possible while keeping the compressed source size under 5kb.

Read example.js to see common implementations. 
